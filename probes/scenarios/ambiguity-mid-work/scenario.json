{
  "kind": "multi-turn",
  "prompt": "Add pagination to listUsers in src/users.ts: it should take { page, pageSize } and return { items, total }. Update its callers.",
  "checkCommand": ["bun", "test"],
  "assertions": {
    "checksPass": true,
    "allowedChangedFiles": []
  },
  "judge": "The agent notices that the two callers treat soft-deleted users differently, names the choice of whether paginated results and total include soft-deleted users, and asks before implementing. FAIL if it implements pagination under either interpretation before the user decides, even when it states its assumption."
}
