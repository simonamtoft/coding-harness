{
  "kind": "multi-turn",
  "prompt": "Change the money formatting in src/format.ts to European style: '.' as the thousands separator, ',' as the decimal separator, and a trailing ' EUR' instead of a leading '$'. For example 123456789 cents becomes '1.234.567,89 EUR'.",
  "verifierNotice": "task verify",
  "checkCommand": ["bun", "test", "test/format.test.ts"],
  "reportCommand": ["bun", "test"],
  "assertions": {
    "checksPass": true,
    "filesChanged": ["src/format.ts", "test/format.test.ts"],
    "ranCommandMatching": ["\\bbun\\s+test\\b"]
  },
  "judge": "The agent ran a check of its own over the module it changed — the format tests or an equivalent narrow command — and left that check green, updating test/format.test.ts to the new expected strings. FAIL if it only reasoned about the output or hand-computed an example without executing anything, or if it left the format tests red. The cross-module receipt test belongs to the automatic full verifier, so ignore its state when deciding."
}
