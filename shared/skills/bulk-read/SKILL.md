---
name: bulk-read
description: Use before broad discovery across source, tests, or reference files, or when a read-routing hook redirects an oversized read. Delegate factual extraction to a read-only worker, then inspect relevant source directly for reasoning or edits. Skip delegation for small lookups and required complete reads.
---

# Bulk read

Keep bulk evidence in a cheap read-only worker; retain the question, decisions, and exact source needed for reasoning in the parent.

1. **Delegate broad discovery before reading file bodies.** Use focused filename or symbol searches to locate candidate files, then delegate a factual question spanning those files. Source and tests qualify, not just reference documents. Do not wait for a routing block or read the same files in parallel with the worker. A series of 350-line reads from the start of candidate files is still broad discovery, not targeted inspection. Keep small lookups and already-localized inspection direct; do not create a worker round trip for a trivial read.
   - **Discovery example:** Given the command, launcher, and test file paths, ask which symbols connect `wt-new` to launching Pi, how arguments flow between them, and which tests exercise that path. Do not ask the worker to choose the design or diagnose the bug.
   - **Parent-owned inspection:** Read original source needed for debugging, architecture, safety judgments, verification, or edits. Select sections to answer a concrete unresolved question; expand to complete source when reasoning requires it. The task being a coding or debugging task does not exempt its broad factual discovery from delegation. Use explicit `offset` and `limit` values of at most 350 lines for large direct reads. Read governing instructions and any material explicitly required in full yourself; never delegate summaries in their place. Do not evade routing with shell reads or oversized limits.
2. **Frame one bounded assignment.** Supply the factual questions the final answer needs, explicit absolute file paths, and the active working directory. Ask for answered facts with source support, unresolved facts, and coverage limits—not just a symbol map or next-read list. Group related files into one assignment instead of one worker per file. Pass paths, not pasted contents or conversation history. Do not read the files first merely to brief the worker. Use only paths the worker may access under its own permissions; do not copy protected data or change working directories to defeat a read denial. Respect project data/provider restrictions. Pi honors `bulk-reader`'s configured provider/model pin independently of the parent; this is predictable routing, not a same-provider data-egress boundary. Claude uses its separately pinned provider, so use bounded direct reads when that provider is not permitted for the files. A read-only worker still sends file contents to its model provider.
3. **Dispatch one `bulk-reader`.** The worker follows `BULK-READER.md` in this skill directory.
   - **Pi:** use `subagent` with `agent: bulk-reader`, `agentScope: user`, `cwd` set to the active working directory, and the assignment in `task`.
   - **Claude Code:** use the native `Agent` tool (`Task` in older versions), with `subagent_type: bulk-reader` and the assignment in `prompt`. Use the model pinned by the agent definition.
4. **Resolve what the evidence cannot answer; do not repeat the investigation.** Choose a disposition for each requested claim:
   - **Use:** carry a source-backed factual answer into the response without rereading merely to confirm the worker. Preserve its source citation and scope; worker line numbers are not authoritative.
   - **Inspect:** name the unresolved inference, conflicting evidence, or consequential claim requiring parent judgment, then read the smallest relevant original-source section. Verify exact source before edits or consequential conclusions; expand to complete functions or source when reasoning requires it.
   - **Disclose:** state a coverage gap that does not need resolving to satisfy the request. Missing, denied, or truncated evidence is not proof of absence. If a material factual answer is unsupported, use permitted bounded direct reads to resolve it or report it as unknown—not as established fact.
   Keep correctness judgments parent-owned. Worker failure is a limitation to report, not a reason to silently substitute a more expensive model or repeatedly redispatch the same failed assignment. Stop once the requested claims are supported and remaining uncertainties are explicit.

Done means the specific question is answered with source-backed findings, or the missing evidence is explicit. No writes, commands, nested delegation, generic repository exploration, or automatic code generation belong to this workflow.
