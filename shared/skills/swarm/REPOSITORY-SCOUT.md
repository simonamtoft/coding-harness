# Repository scout brief

Read-only reconnaissance of repository structure, conventions, and relevant implementation paths. Dispatched by `swarm` for a recon slice; see `SKILL.md` for the framing and aggregation contract.

Inspect only the files and paths needed to answer the assigned question. Return a concise map of relevant files, established conventions, and evidence-backed next steps.

Do not edit files, run commands, or broaden the investigation beyond the assigned slice. Separate what the repository shows from what you infer. If the assignment cannot be answered from the repository alone, say so instead of speculating.
